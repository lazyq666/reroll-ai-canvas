# 首次设置与逐服务连接（LAZ-12）

- Status：Implemented，等待真实供应商凭据与跨平台目录选择器验收后毕业。
- Feature ID：F01 / F02 / F10 / F13。
- Owners：产品、UI、交互、前后端。
- Last verified：2026-09-21。
- Issue：[GitHub #116](https://github.com/lazyq666/reroll-ai-canvas/issues/116)、[Linear LAZ-12](https://linear.app/lazyq/issue/LAZ-12)。
- 边界：[ADR-0001](../adr/0001-workspace-data-boundary.md)、[存储路径与旧数据迁移](../current/storage-layout-and-migration.md)。

## 目标与范围

首次使用者不用先理解 Provider、Protocol 和模型管理，就能连接已有的 AI 服务并开始创作。顺序为「创建管理员 → 选择作品保存位置 → 选择 AI 服务 → 逐个连接 → 准备完成」。不新增 Welcome 页，不要求显示名称，不预选任何服务。LAZ-62 调整完成门槛：管理员与作品保存位置必须完成，AI 服务允许稍后配置；零服务也可进入 Reroll。

此次不改变已有用户登录、设计师申请审批、Workspace 迁移、生成执行或高级模型管理。模型默认策略为：仅启用本次扫描得到的 GPT Image、Nano Banana 图片模型（含已知 Gemini 图片模型别名），Seedance 2.0 及以上视频模型，以及 GPT 5.5 及以上和每个服务中版本最新的两款 Gemini 文本模型。Gemini 同版本同档位的日期快照合并，优先稳定别名；同版本按 Pro、Flash、Flash Lite 排序。Antigravity CLI 例外保留本机发现的 `auto` 图片和文本模型，由 CLI 选择实际型号；安装、版本及登录检查仍按原流程执行，其他服务不适用此例外。引导重新连接成功时替换该服务启用清单，其他服务保持原样；无匹配模型时提示并允许跳过，不回退为全部启用。模型仍由现有发现与能力审核流程分类；此处不增加另一套分类规则。用户随后可在 API 设置和可用模型管理中调整。

## 界面与交互

1. 管理员表单先暂存在页面内存。位置检查通过后，复用原接口一起创建 Workspace 和管理员并自动登录。刷新前未提交的密码不持久化。
2. 保存位置默认沿用已配置位置，否则建议 `~/Documents/Reroll`。生成结果（图片、视频）、生成记录、画布和项目、创作资产使用四组 Lucide 图标与标题。弱提示说明账号、密钥、Token、CLI 登录状态和本机地址不随作品目录迁移。选择其他位置的文件夹图标位于输入框内，使用无背景、无阴影的 ghost 样式，返回使用带可访问名称的箭头按钮。
3. 用户点击使用位置后先执行只读 Inspect；位置不存在时，通过本机专用接口准备空目录并重新检查。创建前检查路径边界、可写性和存储类型，不写入内容或改变当前工作区。非空、目录不可用、不支持的存储位置、与源码重叠均沿用原拒绝规则。发现已有 Workspace 时先显示打开分支。工作区准备后重启，恢复到选择服务。
4. API 服务包含 APIMart、ModelScope、RunningHub、火山引擎和其他 API；CLI 包含即梦、GPT CLI、Antigravity CLI。服务选项复用批量生成页的 `ic-checkbox` list 样式，仅显示图标与名称，不显示名称下方的描述。多选后按选择顺序连接，一次仅展示一个表单。APIMart 只要求 Key。其他 API 要求名称、URL 和 Key，自动识别失败才展示协议选择。
5. API 按「保存 → 验证 → 获取与分类模型 → 启用」执行。成功自动进入下一项；失败保留当前页输入，允许重试或稍后配置，不影响其他已连接服务。
6. CLI 自动检测安装、版本和登录。未安装提供安装指南；已安装但未登录提供命令或即梦扫码入口，并轮询检测。未知登录状态不能当作成功。GPT CLI 没有图片 helper 时不计入图片模型；仍可用已验证的文本来源完成。
7. 服务选择页始终提供结束服务选择的入口：零来源时显示「稍后配置」，已有来源时显示完成入口；即使已勾选服务，也可直接进入完成页，无需逐项跳过。逐服务页面保留单项「稍后配置」，全部跳过或失败后仍可完成。零来源完成页显示「基础设置已完成」，说明可以创建画布、导入和整理素材，AI 生成需后续在「API 设置」连接服务。完成页统一提供「进入 Reroll」主按钮与「配置 AI 服务」次按钮；点击主按钮由后端持久结束引导并直接进入 `/static/canvas-list.html`，不增加确认弹窗或演示页。已保存服务保持原样，跳过不代表服务连接成功。

生产页面使用公开 `ic-*` 组件、共享视觉变量和中英文资源，支持 Light/Dark 与键盘输入。无演示场景切换器。获批 Demo 保存在本地快照分支 `codex/laz-12-approved-demo`（`dc82e69`），不作为正式代码发布。

## 状态、权限与恢复

`onboarding_progress` 位于 Instance State 的 `auth.db`，记录 pending、服务配置指纹和发起安装目录的 scope。不保存明文密钥、密码或供应商账号输出。只有本次 `/api/setup` 创建的管理员会开启进度；旧安装无记录时不强制引导，共用账号库的其他源码目录也不被重定向。旧单行进度表通过事务性增加 scope 列升级，保留原进度。

已登录管理员访问首页或画布列表且本安装 pending 时返回 `/setup`。刷新或进程重启后从服务选择恢复，已经验证的服务保留；选择队列仅按用户保存服务 ID，敏感输入只存在内存。请求结果丢失时重新读取服务器状态，避免重复创建管理员。连接、轮询等请求遇到本机 HTTP 401 时清除页面内敏感输入并返回登录，不误报供应商连接失败；重新登录后恢复服务选择，已保存的队列与已连接服务保留。已建号但未完成工作区重启时保留重启入口。

服务在检查期间若被另一页面修改，最终保存拒绝覆盖。完成时重新比较配置及凭据指纹；被修改或禁用的服务不能用作完成依据。正常连接完成使用 `intent=connected`（默认），仍要求至少一个有效来源；用户在零来源完成页主动进入时发送 `intent=defer`，允许无来源结束引导，不伪造连接结果。配置失效时先刷新服务状态，提示重新连接或稍后配置，不能悄悄把正常完成降级为跳过。两条路径复用本安装 scope 的持久 pending 状态，刷新、重新登录、重启不再强制引导。连接判断代表连接和发现成功，不保证供应商额度、所有模型授权或真实生成始终成功。

## API 与数据归属

服务连接接口只允许本机管理员；目录准备只允许未建号时的本机请求。写操作均验证同源。

| 接口 | 用途 |
| --- | --- |
| `POST /api/setup/prepare-directory` | 仅未建号时可用；本机、同源请求确认后准备位置，拒绝源码重叠及不支持的存储 |
| `GET /api/admin/onboarding` | 获取本安装进度、已连接来源与作品位置，不返回凭据或指纹 |
| `GET /api/admin/onboarding/cli/{service}` | 获取安装、版本与登录布尔状态，不返回 CLI 原始输出 |
| `POST /api/admin/onboarding/connect` | 串联保存、验证和模型发现，通过 NDJSON 返回阶段和稳定错误码 |
| `POST /api/admin/onboarding/complete` | `intent` 为 `connected`（默认）时重新检查来源；`defer` 显式稍后配置允许零来源。均结束本安装引导并返回画布列表地址；未知 intent 拒绝 |

编排位于 `infinite_canvas/onboarding.py`；`main.py` 只组合已有配置保存、Provider 检测和模型发现。API Key 沿用 Device State 密钥存储，模型列表沿用现有配置归属，不写进浏览器存储或作品目录。连接异常只返回稳定错误码，不回传可能含密钥的供应商异常文本。

RunningHub 使用[官方账户状态接口](https://www.runninghub.cn/runninghub-api-doc-cn/api-425748943)验证 Key，不能用公共模型注册表成功代替鉴权。Codex 使用 `login status`。Antigravity 使用 CLI 自行处理的只读 `--print /usage`，不发起模型生成；无法识别报告时维持未知状态，依据见[官方非交互模式说明](https://antigravity.google/docs/cli/headless/)。即梦复用现有登录、额度与版本检查。

## 验证与毕业条件

2026-09-21 自动回归共 179 项通过；`node static/js/i18n/validate-i18n.js` 通过（3748 个键），`python3 scripts/sync_infinite_canvas_ui_version.py --check` 通过，`git diff --check` 无异常。

- HTTP 自动测试覆盖创建与恢复、旧安装不受影响、共享账号库的安装隔离、旧进度升级、本机管理员与跨站保护、无来源正常完成被拒绝、显式稍后配置允许完成、模型为空、密钥变更失效、多服务互不覆盖、错误脱敏及 CLI 登录状态。
- 真实保存适配器使用临时目录验证配置和密钥写入，模拟上游网络，验证多服务合并、并发更改保护和 RunningHub 鉴权。
- 正式页面已在内置浏览器验证账号输入、独立位置页、位置输入框内图标、重启后继续、服务多选、错误保留输入、自定义 API 协议选择、成功进入下一项、CLI 未安装、即梦登录轮询（模拟）、跳过、零来源阻止完成、Ready、语言切换、深色模式和键盘确认直接进入画布列表。测试服务使用临时账号与模拟供应商，不是生产供应商验收。
- 自动入口：`tests/test_onboarding.py`、`tests/test_onboarding_adapters.py`、`tests/test_onboarding_cli_status.py`、`tests/test_account_setup_ui.py`、`tests/test_setup_page_contract.py`。页面 fixture：`SETUP_PREVIEW=1 node tests/setup_browser_smoke.cjs`；独立浏览器 smoke 入口仍保留，当前这次验证由内置浏览器执行。
- 毕业前仍需：真实 API Key 的连接及生成、真实即梦扫码与 CLI 登录、Windows/macOS 原生目录选择与实际进程重启、窄屏人工验收。未使用用户现有密钥、账号或作品目录执行这些测试，因此保持 Active，不宣称 Current 或已发布。

2026-09-22 补充回归：`node --test tests/account_setup_session.test.cjs` 验证本机登录失效跳转、其他受保护请求的登录恢复，以及供应商错误不触发退出登录。此次实测失败日志为本机连接接口 HTTP 401，未到达 APIMart 上游。

2026-09-22 第二轮自动验收：49 项后端/页面合同测试与 3 项前端会话恢复测试通过；新增生产适配器回归覆盖错误 Key 后修正、完整应用重载、300 个模拟型号仅启用 6 个推荐型号，以及失败重连不影响其他服务。上游响应使用模拟数据，未执行真实供应商生成。Chrome 通过 Computer 在独立 8881 环境确认短密码被阻止；随后 Mac 锁定，剩余页面和原生目录选择器验收未完成。

2026-09-22 Antigravity CLI 规则调整：按用户确认，仅对服务 ID 为 `gemini-cli` 的本机来源保留 `auto` 图片和文本模型，允许通过引导；`auto` 不代表某个固定 Gemini 型号。其他 HTTP 或 CLI 服务的 `auto` 仍不自动启用。未安装、版本过旧、未登录或登录状态未知时仍阻止连接。回归直接使用 `cli_impl.gemini_cli_models_payload()` 的实际目录，验证连接成功、启用清单、完成引导及上述拒绝边界；本机状态与连接响应使用模拟数据，不代表已验证真实 CLI 生成。本次 34 项后端回归与 7 项文档检查通过。

2026-09-22 Chrome + Computer 补充验收：在独立 8881 真实环境验证短密码、密码不一致均被阻止，修正后进入位置页。系统目录请求最终返回 HTTP 200，但对话框取消与所选路径未取得完整 UI 证据，保持待验收。在 8879 页面 fixture 验证目录错误后恢复、选择结果写入、多选计数、全部跳过后的零来源阻断、错误 Key 保留输入及修正后自动进入下一项、第二服务失败不影响第一服务、刷新保留已连接服务、英文深色完成页，以及 Tab/Enter 完成并跳转到画布列表地址。fixture 最终 pending=false 且仅保留 APIMart 成功记录；不把此结果等同于真实上游鉴权或生成成功。完成后重访的服务器规则由自动回归覆盖，未追加浏览器重访证据。用户 8880 数据未用于本轮浏览器测试。

## LAZ-62：AI 服务可稍后配置

取代 LAZ-12 最初的“至少一个来源才能结束引导”产品门槛；上文旧日期的零来源阻断验收仅记录历史行为。基础账号、工作区校验及本机管理员和同源保护保持不变。后续服务配置沿用现有 API 设置；无模型的生成入口沿用现有配置提示，设计师沿用联系管理员提示。

回归覆盖零来源主动跳过、失败后跳过、部分成功保留配置、重复完成、重新登录和重新读取持久状态、非法 intent、正常完成时凭据变化失效，以及匿名、设计师、远程与跨站请求拒绝。页面验收覆盖零选择直达完成页、全部跳过、失败恢复、中英文切换、Light/Dark、窄屏、键盘完成与完成后重访。

2026-09-23 独立真实后端补验：8793 端口运行正式 `main.app`，Workspace、Instance State、Device State、Cache 均隔离在独立临时目录。通过正式页面提交管理员与工作区设置，实际停止并重新启动后端后恢复服务选择；零选择点击「稍后配置 → 进入 Reroll」成功进入真实画布列表，并经 UI 创建和打开智能画布。使用真实 `/api/ai/upload` 导入测试 PNG，再通过正式 WebSocket Canvas Mutation 创建 Image Node；第二次实际进程重启后，重新登录与退出后再登录均成功，首页和画布列表返回 200，pending=false、已连接服务=0、画布 revision=1、节点=1、媒体可读取，浏览器确认图像节点正常显示。正式首页「设置 → API 设置」可打开补配页面。此环境直接运行后端，不含桌面启动器，页面自动重启接口不可用，因此由测试脚本执行进程停止/启动；不把该结果当作桌面启动器或原生目录选择器验收。未配置真实供应商密钥或执行收费生成。

2026-09-23 LAZ-62 本地验证：`PYTHONPATH=backend .venv/bin/python -m unittest tests.test_onboarding tests.test_onboarding_adapters tests.test_onboarding_cli_status tests.test_account_setup_ui tests.test_setup_page_contract` 共 43 项通过；`node --test tests/account_setup_session.test.cjs` 共 6 项通过（含重复点击防重与来源失效不自动跳过）；文档知识图、i18n 缓存和核心文案测试共 14 项通过。`node static/js/i18n/validate-i18n.js` 通过（3808 keys），统一前端资源同步与 `--check`、`git diff --check` 通过。内置浏览器使用独立 8792 页面 fixture，验证零选择直接进入完成页、返回配置、多选、连接失败后逐项全跳过、中英文动态切换、英文 Dark 375px 无横向溢出、Enter 完成并进入画布列表地址、完成后重访不再阻断；控制台无错误。浏览器上游与画布列表为 fixture；真实后端持久化、权限、来源指纹与重新登录由 HTTP 自动测试覆盖。未运行独立 Chrome smoke（已更新场景并通过语法检查），未执行真实供应商生成或原生进程重启；LAZ-12 原有外部服务与跨平台验收继续待完成。
